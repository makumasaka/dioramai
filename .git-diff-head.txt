diff --git a/apps/demo-export/package.json b/apps/demo-export/package.json
index 1061109..3779f69 100644
--- a/apps/demo-export/package.json
+++ b/apps/demo-export/package.json
@@ -14,7 +14,8 @@
     "@react-three/fiber": "^9.6.0",
     "react": "^19.2.5",
     "react-dom": "^19.2.5",
-    "three": "^0.184.0"
+    "three": "^0.184.0",
+    "three-stdlib": "^2.36.1"
   },
   "devDependencies": {
     "@types/react": "^19.2.14",
diff --git a/apps/demo-export/src/generated/DioramaiScene.generated.tsx b/apps/demo-export/src/generated/DioramaiScene.generated.tsx
index 05b1bfe..054a63f 100644
--- a/apps/demo-export/src/generated/DioramaiScene.generated.tsx
+++ b/apps/demo-export/src/generated/DioramaiScene.generated.tsx
@@ -3,6 +3,7 @@
 /* This file is owned by Dioramai. Edit dioramaiScene for MVP code -> runtime sync. */
 import { Suspense, useMemo } from 'react';
 import { useGLTF } from '@react-three/drei';
+import { SkeletonUtils } from 'three-stdlib';
 
 type Vec3 = readonly [number, number, number];
 type DioramaiNode = {
@@ -158,7 +159,7 @@ function isRenderableAssetUri(uri: string | undefined): string | undefined {
 
 function AssetModel({ uri }: { uri: string }) {
   const gltf = useGLTF(uri);
-  const object = useMemo(() => gltf.scene.clone(true), [gltf.scene]);
+  const object = useMemo(() => SkeletonUtils.clone(gltf.scene), [gltf.scene]);
   return <primitive object={object} />;
 }
 
diff --git a/apps/web/package.json b/apps/web/package.json
index bc6d473..936c2d2 100644
--- a/apps/web/package.json
+++ b/apps/web/package.json
@@ -20,6 +20,7 @@
     "react": "^19.2.5",
     "react-dom": "^19.2.5",
     "three": "^0.184.0",
+    "three-stdlib": "^2.36.1",
     "zustand": "^5.0.12"
   },
   "devDependencies": {
diff --git a/apps/web/src/ui/CodePane.test.tsx b/apps/web/src/ui/CodePane.test.tsx
index b6d046e..e5e70f6 100644
--- a/apps/web/src/ui/CodePane.test.tsx
+++ b/apps/web/src/ui/CodePane.test.tsx
@@ -18,6 +18,7 @@ vi.mock('../bridge/bridgeClient', async () => ({
 
 describe('CodePane sync controls', () => {
   beforeEach(() => {
+    useSceneStore.getState().setBridgeStatus(false, null);
     useSceneStore.getState().reset();
     useSceneStore.getState().setBridgeStatus(true, null);
     vi.clearAllMocks();
diff --git a/apps/web/src/viewport/NodeMesh.test.tsx b/apps/web/src/viewport/NodeMesh.test.tsx
index 5de2a2c..6a5f3eb 100644
--- a/apps/web/src/viewport/NodeMesh.test.tsx
+++ b/apps/web/src/viewport/NodeMesh.test.tsx
@@ -5,6 +5,10 @@ import { getStarterScene } from '@dioramai/core';
 import { useSceneStore } from '../store/sceneStore';
 import { NodeMesh } from './NodeMesh';
 
+const skeletonClone = vi.hoisted(() =>
+  vi.fn((object: unknown) => ({ type: 'skeleton-safe-clone', source: object })),
+);
+
 interface TransformControlsTestRef {
   addEventListener: (
     name: 'dragging-changed',
@@ -55,6 +59,12 @@ vi.mock('@react-three/drei', async () => {
   };
 });
 
+vi.mock('three-stdlib', () => ({
+  SkeletonUtils: {
+    clone: skeletonClone,
+  },
+}));
+
 vi.mock('./object3dTransform', () => ({
   transformPatchFromObject3D: () => ({ position: [6, 7, 8] }),
 }));
@@ -91,6 +101,7 @@ describe('NodeMesh command adapter', () => {
       originalError(...args);
     });
     useSceneStore.getState().reset();
+    skeletonClone.mockClear();
   });
 
   afterEach(() => {
@@ -188,6 +199,7 @@ describe('NodeMesh command adapter', () => {
 
     expect(container.querySelector('primitive')).not.toBeNull();
     expect(container.querySelector('boxGeometry')).toBeNull();
+    expect(skeletonClone).toHaveBeenCalledTimes(1);
   });
 
   it('keeps the proxy mesh for unsafe asset refs', () => {
diff --git a/apps/web/src/viewport/NodeMesh.tsx b/apps/web/src/viewport/NodeMesh.tsx
index 4916941..84b8230 100644
--- a/apps/web/src/viewport/NodeMesh.tsx
+++ b/apps/web/src/viewport/NodeMesh.tsx
@@ -11,7 +11,7 @@ import {
 } from 'react';
 import { TransformControls, useGLTF } from '@react-three/drei';
 import type { ThreeEvent } from '@react-three/fiber';
-import type { TransformControls as TransformControlsImpl } from 'three-stdlib';
+import { SkeletonUtils, type TransformControls as TransformControlsImpl } from 'three-stdlib';
 import type { Group, Object3D } from 'three';
 import { useShallow } from 'zustand/react/shallow';
 import { useSceneStore } from '../store/sceneStore';
@@ -46,7 +46,7 @@ const resolveRenderableAssetUri = (uri: string | undefined): string | undefined
 
 function AssetModel({ uri }: { uri: string }) {
   const gltf = useGLTF(uri);
-  const object = useMemo(() => gltf.scene.clone(true), [gltf.scene]);
+  const object = useMemo(() => SkeletonUtils.clone(gltf.scene), [gltf.scene]);
   return <primitive object={object} />;
 }
 
diff --git a/apps/web/src/viewport/runtimeSceneGizmo.test.tsx b/apps/web/src/viewport/runtimeSceneGizmo.test.tsx
index 1b945cd..2079527 100644
--- a/apps/web/src/viewport/runtimeSceneGizmo.test.tsx
+++ b/apps/web/src/viewport/runtimeSceneGizmo.test.tsx
@@ -8,6 +8,9 @@ import { RuntimeScene } from '@dioramai/r3f-bridge';
 // RefObject is a regression: drei reads `ref.current` once and never
 // re-attaches, leaving the gizmo bound to a stale, detached group.
 const capturedObjects: unknown[] = [];
+const skeletonClone = vi.hoisted(() =>
+  vi.fn((object: unknown) => ({ type: 'skeleton-safe-clone', source: object })),
+);
 
 vi.mock('@react-three/drei', async () => {
   const React = await import('react');
@@ -28,12 +31,19 @@ vi.mock('@react-three/drei', async () => {
   };
 });
 
+vi.mock('three-stdlib', () => ({
+  SkeletonUtils: {
+    clone: skeletonClone,
+  },
+}));
+
 describe('RuntimeScene gizmo attachment', () => {
   let consoleError: ReturnType<typeof vi.spyOn>;
   let originalError: typeof console.error;
 
   beforeEach(() => {
     capturedObjects.length = 0;
+    skeletonClone.mockClear();
     originalError = console.error;
     consoleError = vi.spyOn(console, 'error').mockImplementation((...args) => {
       const message = args.map((arg) => String(arg)).join(' ');
@@ -58,6 +68,34 @@ describe('RuntimeScene gizmo attachment', () => {
     consoleError.mockRestore();
   });
 
+  it('uses a skeleton-safe clone for GLB asset nodes', () => {
+    const scene = getStarterScene('default');
+    const childId = scene.nodes[scene.rootId]!.children[0]!;
+    const child = scene.nodes[childId]!;
+
+    const { container } = render(
+      <RuntimeScene
+        scene={{
+          ...scene,
+          nodes: {
+            ...scene.nodes,
+            [childId]: {
+              ...child,
+              assetRef: { kind: 'uri', uri: '/assets/imports/android.glb' },
+            },
+          },
+        }}
+        selectedId={childId}
+        gizmoMode="translate"
+        onCommand={() => undefined}
+        onSelect={() => undefined}
+      />,
+    );
+
+    expect(container.querySelector('primitive')).not.toBeNull();
+    expect(skeletonClone).toHaveBeenCalled();
+  });
+
   it('passes the live group instance (not a RefObject) to TransformControls', () => {
     const scene = getStarterScene('default');
     const childId = scene.nodes[scene.rootId]!.children[0]!;
diff --git a/package-lock.json b/package-lock.json
index 5c163a2..52351fe 100644
--- a/package-lock.json
+++ b/package-lock.json
@@ -20,7 +20,8 @@
         "@react-three/fiber": "^9.6.0",
         "react": "^19.2.5",
         "react-dom": "^19.2.5",
-        "three": "^0.184.0"
+        "three": "^0.184.0",
+        "three-stdlib": "^2.36.1"
       },
       "devDependencies": {
         "@types/react": "^19.2.14",
@@ -42,6 +43,7 @@
         "react": "^19.2.5",
         "react-dom": "^19.2.5",
         "three": "^0.184.0",
+        "three-stdlib": "^2.36.1",
         "zustand": "^5.0.12"
       },
       "devDependencies": {
@@ -5978,7 +5980,7 @@
     },
     "packages/cli": {
       "name": "dioramai",
-      "version": "0.1.2",
+      "version": "0.1.6",
       "license": "MIT",
       "bin": {
         "diorama": "dist/index.js",
@@ -6090,7 +6092,8 @@
         "@react-three/drei": "^10.7.7",
         "@react-three/fiber": "^9.6.0",
         "react": "^19.2.5",
-        "three": "^0.184.0"
+        "three": "^0.184.0",
+        "three-stdlib": "^2.36.1"
       },
       "devDependencies": {
         "@types/react": "^19.2.14",
diff --git a/packages/export-r3f/src/__snapshots__/mcpLiteShowroomExport.test.ts.snap b/packages/export-r3f/src/__snapshots__/mcpLiteShowroomExport.test.ts.snap
index 24f195b..6e7a6e4 100644
--- a/packages/export-r3f/src/__snapshots__/mcpLiteShowroomExport.test.ts.snap
+++ b/packages/export-r3f/src/__snapshots__/mcpLiteShowroomExport.test.ts.snap
@@ -621,7 +621,7 @@ export function McpLiteShowroom() {
           scale={[8, 3.2, 0.12]}
           renderMesh
         />
-        {/* product_01 - Product 01 | role=product | group=display_area | traits=clickable,hoverable,displayable,focusable | behavior=click_select,hover_highlight,show_info,focus_camera */}
+        {/* product_01 - Product 01 | role=product | group=display_area | traits=clickable,hoverable,displayable,focusable | behaviors=click_select,hover_highlight,show_info,focus_camera */}
         <Product
           sourceId="product_01"
           name="Product 01"
@@ -639,7 +639,7 @@ export function McpLiteShowroom() {
         {selectedId === 'product_01' ? (
           <>{/* TODO: render info panel for Product information: Interactive showroom item generated from structured scene metadata.. */}</>
         ) : null}
-        {/* bench - Bench | role=seating | group=seating_area | traits=seatable,focusable | behavior=focus_camera,anchor_point */}
+        {/* bench - Bench | role=seating | group=seating_area | traits=seatable,focusable | behaviors=focus_camera,anchor_point */}
         {/* TODO: anchor_point is scaffolded as a developer hook; no runtime behavior is generated. */}
         <SeatingElement
           sourceId="bench"
@@ -650,7 +650,7 @@ export function McpLiteShowroom() {
           renderMesh
           onSelect={() => handleFocusCamera('bench')}
         />
-        {/* display_table - Display Table | role=display | group=display_area | traits=displayable | behavior=show_info */}
+        {/* display_table - Display Table | role=display | group=display_area | traits=displayable | behaviors=show_info */}
         <DisplaySurface
           sourceId="display_table"
           name="Display Table"
@@ -662,7 +662,7 @@ export function McpLiteShowroom() {
         {selectedId === 'display_table' ? (
           <>{/* TODO: render info panel for Display Table. */}</>
         ) : null}
-        {/* product_03 - Product 03 | role=product | group=display_area | traits=clickable,hoverable,displayable,focusable | behavior=click_select,hover_highlight,show_info,focus_camera */}
+        {/* product_03 - Product 03 | role=product | group=display_area | traits=clickable,hoverable,displayable,focusable | behaviors=click_select,hover_highlight,show_info,focus_camera */}
         <Product
           sourceId="product_03"
           name="Product 03"
@@ -690,7 +690,7 @@ export function McpLiteShowroom() {
           lightKind="ambient"
           lightIntensity={0.35}
         />
-        {/* chair - Chair | role=seating | group=seating_area | traits=seatable,focusable | behavior=focus_camera,anchor_point */}
+        {/* chair - Chair | role=seating | group=seating_area | traits=seatable,focusable | behaviors=focus_camera,anchor_point */}
         {/* TODO: anchor_point is scaffolded as a developer hook; no runtime behavior is generated. */}
         <SeatingElement
           sourceId="chair"
@@ -701,7 +701,7 @@ export function McpLiteShowroom() {
           renderMesh
           onSelect={() => handleFocusCamera('chair')}
         />
-        {/* display_plinth - Display Plinth | role=display | group=display_area | traits=displayable | behavior=show_info */}
+        {/* display_plinth - Display Plinth | role=display | group=display_area | traits=displayable | behaviors=show_info */}
         <DisplaySurface
           sourceId="display_plinth"
           name="Display Plinth"
@@ -713,7 +713,7 @@ export function McpLiteShowroom() {
         {selectedId === 'display_plinth' ? (
           <>{/* TODO: render info panel for Display Plinth. */}</>
         ) : null}
-        {/* product_02 - Product 02 | role=product | group=display_area | traits=clickable,hoverable,displayable,focusable | behavior=click_select,hover_highlight,show_info,focus_camera */}
+        {/* product_02 - Product 02 | role=product | group=display_area | traits=clickable,hoverable,displayable,focusable | behaviors=click_select,hover_highlight,show_info,focus_camera */}
         <Product
           sourceId="product_02"
           name="Product 02"
diff --git a/packages/export-r3f/src/__snapshots__/milestone7ExportEval.test.ts.snap b/packages/export-r3f/src/__snapshots__/milestone7ExportEval.test.ts.snap
index 71c069b..34de712 100644
--- a/packages/export-r3f/src/__snapshots__/milestone7ExportEval.test.ts.snap
+++ b/packages/export-r3f/src/__snapshots__/milestone7ExportEval.test.ts.snap
@@ -1250,7 +1250,7 @@ export function InteractiveShowroom() {
           scale={[8, 3.2, 0.12]}
           renderMesh
         />
-        {/* product_01 - Product 01 | role=product | group=display_area | traits=clickable,hoverable,displayable,focusable | behavior=click_select,hover_highlight,show_info,focus_camera */}
+        {/* product_01 - Product 01 | role=product | group=display_area | traits=clickable,hoverable,displayable,focusable | behaviors=click_select,hover_highlight,show_info,focus_camera */}
         <Product
           sourceId="product_01"
           name="Product 01"
@@ -1268,7 +1268,7 @@ export function InteractiveShowroom() {
         {selectedId === 'product_01' ? (
           <>{/* TODO: render info panel for Product information: Interactive showroom item generated from structured scene metadata.. */}</>
         ) : null}
-        {/* bench - Bench | role=seating | group=seating_area | traits=seatable,focusable | behavior=focus_camera,anchor_point */}
+        {/* bench - Bench | role=seating | group=seating_area | traits=seatable,focusable | behaviors=focus_camera,anchor_point */}
         {/* TODO: anchor_point is scaffolded as a developer hook; no runtime behavior is generated. */}
         <SeatingElement
           sourceId="bench"
@@ -1279,7 +1279,7 @@ export function InteractiveShowroom() {
           renderMesh
           onSelect={() => handleFocusCamera('bench')}
         />
-        {/* display_table - Display Table | role=display | group=display_area | traits=displayable | behavior=show_info */}
+        {/* display_table - Display Table | role=display | group=display_area | traits=displayable | behaviors=show_info */}
         <DisplaySurface
           sourceId="display_table"
           name="Display Table"
@@ -1291,7 +1291,7 @@ export function InteractiveShowroom() {
         {selectedId === 'display_table' ? (
           <>{/* TODO: render info panel for Display Table. */}</>
         ) : null}
-        {/* product_03 - Product 03 | role=product | group=display_area | traits=clickable,hoverable,displayable,focusable | behavior=click_select,hover_highlight,show_info,focus_camera */}
+        {/* product_03 - Product 03 | role=product | group=display_area | traits=clickable,hoverable,displayable,focusable | behaviors=click_select,hover_highlight,show_info,focus_camera */}
         <Product
           sourceId="product_03"
           name="Product 03"
@@ -1319,7 +1319,7 @@ export function InteractiveShowroom() {
           lightKind="ambient"
           lightIntensity={0.35}
         />
-        {/* chair - Chair | role=seating | group=seating_area | traits=seatable,focusable | behavior=focus_camera,anchor_point */}
+        {/* chair - Chair | role=seating | group=seating_area | traits=seatable,focusable | behaviors=focus_camera,anchor_point */}
         {/* TODO: anchor_point is scaffolded as a developer hook; no runtime behavior is generated. */}
         <SeatingElement
           sourceId="chair"
@@ -1330,7 +1330,7 @@ export function InteractiveShowroom() {
           renderMesh
           onSelect={() => handleFocusCamera('chair')}
         />
-        {/* display_plinth - Display Plinth | role=display | group=display_area | traits=displayable | behavior=show_info */}
+        {/* display_plinth - Display Plinth | role=display | group=display_area | traits=displayable | behaviors=show_info */}
         <DisplaySurface
           sourceId="display_plinth"
           name="Display Plinth"
@@ -1342,7 +1342,7 @@ export function InteractiveShowroom() {
         {selectedId === 'display_plinth' ? (
           <>{/* TODO: render info panel for Display Plinth. */}</>
         ) : null}
-        {/* product_02 - Product 02 | role=product | group=display_area | traits=clickable,hoverable,displayable,focusable | behavior=click_select,hover_highlight,show_info,focus_camera */}
+        {/* product_02 - Product 02 | role=product | group=display_area | traits=clickable,hoverable,displayable,focusable | behaviors=click_select,hover_highlight,show_info,focus_camera */}
         <Product
           sourceId="product_02"
           name="Product 02"
diff --git a/packages/export-r3f/src/__snapshots__/r3fModule.test.ts.snap b/packages/export-r3f/src/__snapshots__/r3fModule.test.ts.snap
index 7d54ad6..78d6c47 100644
--- a/packages/export-r3f/src/__snapshots__/r3fModule.test.ts.snap
+++ b/packages/export-r3f/src/__snapshots__/r3fModule.test.ts.snap
@@ -307,7 +307,7 @@ export function DioramaiScene() {
           scale={[8, 3.2, 0.12]}
           renderMesh
         />
-        {/* product_01 - Product 01 | role=product | group=display_area | traits=clickable,hoverable,displayable,focusable | behavior=click_select,hover_highlight,show_info,focus_camera */}
+        {/* product_01 - Product 01 | role=product | group=display_area | traits=clickable,hoverable,displayable,focusable | behaviors=click_select,hover_highlight,show_info,focus_camera */}
         <Product
           sourceId="product_01"
           name="Product 01"
@@ -325,7 +325,7 @@ export function DioramaiScene() {
         {selectedId === 'product_01' ? (
           <>{/* TODO: render info panel for Product information: Interactive showroom item generated from structured scene metadata.. */}</>
         ) : null}
-        {/* bench - Bench | role=seating | group=seating_area | traits=seatable,focusable | behavior=focus_camera,anchor_point */}
+        {/* bench - Bench | role=seating | group=seating_area | traits=seatable,focusable | behaviors=focus_camera,anchor_point */}
         {/* TODO: anchor_point is scaffolded as a developer hook; no runtime behavior is generated. */}
         <SeatingElement
           sourceId="bench"
@@ -336,7 +336,7 @@ export function DioramaiScene() {
           renderMesh
           onSelect={() => handleFocusCamera('bench')}
         />
-        {/* display_table - Display Table | role=display | group=display_area | traits=displayable | behavior=show_info */}
+        {/* display_table - Display Table | role=display | group=display_area | traits=displayable | behaviors=show_info */}
         <DisplaySurface
           sourceId="display_table"
           name="Display Table"
@@ -348,7 +348,7 @@ export function DioramaiScene() {
         {selectedId === 'display_table' ? (
           <>{/* TODO: render info panel for Display Table. */}</>
         ) : null}
-        {/* product_03 - Product 03 | role=product | group=display_area | traits=clickable,hoverable,displayable,focusable | behavior=click_select,hover_highlight,show_info,focus_camera */}
+        {/* product_03 - Product 03 | role=product | group=display_area | traits=clickable,hoverable,displayable,focusable | behaviors=click_select,hover_highlight,show_info,focus_camera */}
         <Product
           sourceId="product_03"
           name="Product 03"
@@ -376,7 +376,7 @@ export function DioramaiScene() {
           lightKind="ambient"
           lightIntensity={0.35}
         />
-        {/* chair - Chair | role=seating | group=seating_area | traits=seatable,focusable | behavior=focus_camera,anchor_point */}
+        {/* chair - Chair | role=seating | group=seating_area | traits=seatable,focusable | behaviors=focus_camera,anchor_point */}
         {/* TODO: anchor_point is scaffolded as a developer hook; no runtime behavior is generated. */}
         <SeatingElement
           sourceId="chair"
@@ -387,7 +387,7 @@ export function DioramaiScene() {
           renderMesh
           onSelect={() => handleFocusCamera('chair')}
         />
-        {/* display_plinth - Display Plinth | role=display | group=display_area | traits=displayable | behavior=show_info */}
+        {/* display_plinth - Display Plinth | role=display | group=display_area | traits=displayable | behaviors=show_info */}
         <DisplaySurface
           sourceId="display_plinth"
           name="Display Plinth"
@@ -399,7 +399,7 @@ export function DioramaiScene() {
         {selectedId === 'display_plinth' ? (
           <>{/* TODO: render info panel for Display Plinth. */}</>
         ) : null}
-        {/* product_02 - Product 02 | role=product | group=display_area | traits=clickable,hoverable,displayable,focusable | behavior=click_select,hover_highlight,show_info,focus_camera */}
+        {/* product_02 - Product 02 | role=product | group=display_area | traits=clickable,hoverable,displayable,focusable | behaviors=click_select,hover_highlight,show_info,focus_camera */}
         <Product
           sourceId="product_02"
           name="Product 02"
diff --git a/packages/export-r3f/src/moduleExporter.ts b/packages/export-r3f/src/moduleExporter.ts
index 86eb178..35fed8b 100644
--- a/packages/export-r3f/src/moduleExporter.ts
+++ b/packages/export-r3f/src/moduleExporter.ts
@@ -107,17 +107,24 @@ const safeWrapperMapFromScene = (
 };
 
 const emitImports = (needsState: boolean, includeAssetLoader: boolean): string => {
-  const reactImports = needsState
-    ? `import { useState } from 'react';\nimport type { ReactNode } from 'react';\n`
+  const reactHooks = [
+    needsState ? 'useState' : null,
+    includeAssetLoader ? 'useMemo' : null,
+  ].filter((hook): hook is string => hook !== null);
+  const reactImports = reactHooks.length > 0
+    ? `import { ${reactHooks.join(', ')} } from 'react';\nimport type { ReactNode } from 'react';\n`
     : `import type { ReactNode } from 'react';\n`;
-  const dreiImport = includeAssetLoader ? `import { useGLTF } from '@react-three/drei';\n` : '';
-  return `${reactImports}import type { ThreeElements } from '@react-three/fiber';\n${dreiImport}\n`;
+  const assetImports = includeAssetLoader
+    ? `import { useGLTF } from '@react-three/drei';\nimport { SkeletonUtils } from 'three-stdlib';\n`
+    : '';
+  return `${reactImports}import type { ThreeElements } from '@react-three/fiber';\n${assetImports}\n`;
 };
 
 const emitAssetModel = (): string =>
   `function AssetModel({ uri }: { uri: string }) {\n` +
   `  const gltf = useGLTF(uri);\n` +
-  `  return <primitive object={gltf.scene.clone()} />;\n` +
+  `  const object = useMemo(() => SkeletonUtils.clone(gltf.scene), [gltf.scene]);\n` +
+  `  return <primitive object={object} />;\n` +
   `}\n\n`;
 
 const emitSemanticNode = (): string =>
diff --git a/packages/export-r3f/src/r3fModule.test.ts b/packages/export-r3f/src/r3fModule.test.ts
index 6f8e159..129e76b 100644
--- a/packages/export-r3f/src/r3fModule.test.ts
+++ b/packages/export-r3f/src/r3fModule.test.ts
@@ -171,7 +171,9 @@ describe('exportSceneToR3fModule', () => {
     };
     const result = exportSceneToR3fModule(scene);
     expect(result.code).toContain("import { useGLTF } from '@react-three/drei';");
+    expect(result.code).toContain("import { SkeletonUtils } from 'three-stdlib';");
     expect(result.code).toContain('function AssetModel');
+    expect(result.code).toContain('SkeletonUtils.clone(gltf.scene)');
     expect(result.code).toContain('assetUri="/assets/generated/chair.glb"');
     expect(result.diagnostics.some((d) => d.code === 'unsafe_asset_uri')).toBe(false);
   });
diff --git a/packages/export-r3f/src/syncModule.test.ts b/packages/export-r3f/src/syncModule.test.ts
index a84683b..d6d7d24 100644
--- a/packages/export-r3f/src/syncModule.test.ts
+++ b/packages/export-r3f/src/syncModule.test.ts
@@ -75,7 +75,9 @@ describe('R3F sync module export', () => {
     const out = exportSceneToR3fSyncModule(scene).code;
 
     expect(out).toContain("import { useGLTF } from '@react-three/drei';");
+    expect(out).toContain("import { SkeletonUtils } from 'three-stdlib';");
     expect(out).toContain('function AssetModel');
+    expect(out).toContain('SkeletonUtils.clone(gltf.scene)');
     expect(out).toContain('/assets/dioramai/product.glb');
     expect(parseSceneFromR3fSyncModule(out)).toEqual({
       ok: true,
diff --git a/packages/export-r3f/src/syncModule.ts b/packages/export-r3f/src/syncModule.ts
index 5e215a3..b52d286 100644
--- a/packages/export-r3f/src/syncModule.ts
+++ b/packages/export-r3f/src/syncModule.ts
@@ -32,7 +32,8 @@ const renderSyncModule = (
   `${DIORAMAI_GENERATED_MARKER}\n` +
   `/* This file is owned by Dioramai. Edit dioramaiScene for MVP code -> runtime sync. */\n` +
   `import { Suspense, useMemo } from 'react';\n` +
-  `import { useGLTF } from '@react-three/drei';\n\n` +
+  `import { useGLTF } from '@react-three/drei';\n` +
+  `import { SkeletonUtils } from 'three-stdlib';\n\n` +
   `type Vec3 = readonly [number, number, number];\n` +
   `type DioramaiNode = {\n` +
   `  id: string;\n` +
@@ -66,7 +67,7 @@ const renderSyncModule = (
   `}\n\n` +
   `function AssetModel({ uri }: { uri: string }) {\n` +
   `  const gltf = useGLTF(uri);\n` +
-  `  const object = useMemo(() => gltf.scene.clone(true), [gltf.scene]);\n` +
+  `  const object = useMemo(() => SkeletonUtils.clone(gltf.scene), [gltf.scene]);\n` +
   `  return <primitive object={object} />;\n` +
   `}\n\n` +
   `function ProxyMesh() {\n` +
diff --git a/packages/local-bridge/src/runtime.ts b/packages/local-bridge/src/runtime.ts
index 9db3d60..cf88a69 100644
--- a/packages/local-bridge/src/runtime.ts
+++ b/packages/local-bridge/src/runtime.ts
@@ -731,6 +731,7 @@ const vitePackageJson = (projectRoot: string): string => jsonFile({
     react: '^19.2.5',
     'react-dom': '^19.2.5',
     three: '^0.184.0',
+    'three-stdlib': '^2.36.1',
   },
   devDependencies: {
     '@types/react': '^19.2.14',
diff --git a/packages/r3f-bridge/package.json b/packages/r3f-bridge/package.json
index 176db0a..83f4efa 100644
--- a/packages/r3f-bridge/package.json
+++ b/packages/r3f-bridge/package.json
@@ -21,7 +21,8 @@
     "@react-three/drei": "^10.7.7",
     "@react-three/fiber": "^9.6.0",
     "react": "^19.2.5",
-    "three": "^0.184.0"
+    "three": "^0.184.0",
+    "three-stdlib": "^2.36.1"
   },
   "devDependencies": {
     "@types/react": "^19.2.14",
diff --git a/packages/r3f-bridge/src/RuntimeScene.tsx b/packages/r3f-bridge/src/RuntimeScene.tsx
index 54e1524..ac1cc58 100644
--- a/packages/r3f-bridge/src/RuntimeScene.tsx
+++ b/packages/r3f-bridge/src/RuntimeScene.tsx
@@ -12,7 +12,7 @@ import { TransformControls, useGLTF } from '@react-three/drei';
 import type { ThreeEvent } from '@react-three/fiber';
 import type { Command, Scene } from '@dioramai/core';
 import type { Group } from 'three';
-import type { TransformControls as TransformControlsImpl } from 'three-stdlib';
+import { SkeletonUtils, type TransformControls as TransformControlsImpl } from 'three-stdlib';
 import {
   commandFromObject3DTransform,
 } from './transformCommand';
@@ -56,7 +56,7 @@ export const isRenderableAssetUri = (uri: string | undefined): string | undefine
 
 function AssetModel({ uri }: { uri: string }) {
   const gltf = useGLTF(uri);
-  const object = useMemo(() => gltf.scene.clone(true), [gltf.scene]);
+  const object = useMemo(() => SkeletonUtils.clone(gltf.scene), [gltf.scene]);
   return <primitive object={object} />;
 }
 
